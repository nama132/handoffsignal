# --- secrets and environment (master prompt section 3.1 denylist) ---
.env
.env.*
!.env.example
CREDENTIALS.md
CREDENTIALS.txt
TOOLS_AND_SERVICES.txt
*.pem
*.key

# --- owner-excluded from version control ---
# The governing specification is deliberately kept out of the repository at the owner's
# direction (Phase 1B). It stays present in the working tree; this rule exists so a blind
# `git add .` can never sweep it into permanent history.
CLAUDE_V2_COMMERCIAL_CLEANING_MASTER_PROMPT.md

# --- local databases / data ---
*.db
*.sqlite3
*.sqlite
*.dump
*.sql.gz

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/

# --- test / quality artifacts ---
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.mypy_cache/
.ruff_cache/
pylock.toml
requirements-audit.txt

# --- uploads, logs, caches, generated demo output ---
media/
uploads/
tmp_imports/
staticfiles/
logs/
*.log

# --- browser automation artifacts (Phase 6/7) ---
test-results/
playwright-report/
.playwright/

# --- editor / OS ---
.DS_Store
.idea/
.vscode/
